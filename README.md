# header_checker
A Python script for pentesting that checks whether key security response headers are present on a given URL, including HSTS, CSP, X-Frame-Options, and others. Outputs a color-coded report with current values and recommended fixes for missing headers.
